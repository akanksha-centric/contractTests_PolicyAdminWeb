#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@pact-foundation/absolute-version/dist/bin/absolute-version-from-git-tag.js" "$@"
  ret=$?
else 
  node  "$basedir/../@pact-foundation/absolute-version/dist/bin/absolute-version-from-git-tag.js" "$@"
  ret=$?
fi
exit $ret
