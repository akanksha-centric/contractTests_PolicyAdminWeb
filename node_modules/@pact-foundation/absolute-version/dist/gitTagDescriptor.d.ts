import { AbsoluteVersionConfig } from 'types';
import { Descriptor } from './output';
export declare const getBranchString: (clean: (s: string) => string) => string;
export declare const gitTagDescriptor: (clean: (s: string) => string, config: AbsoluteVersionConfig) => Descriptor;
